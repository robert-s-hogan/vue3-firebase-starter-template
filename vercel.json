{
  "build": {
    "env": {
      "NODE_ENV": "production",
      "NODE_VERSION": "18.x"
    }
  },

  "installCommand": "pnpm install -g pnpm@latest && pnpm install",
  "buildCommand": "pnpm run build",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
